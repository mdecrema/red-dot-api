import { User } from "../user/entities/user.entity";

export class BaseInterface {

	id?: number;

	// isArchived?: boolean;

	// createdAt?: Date;

	// createdBy?: User['id'];

	// updatedAt?: Date;

	// updatedBy?: User['id'];
}